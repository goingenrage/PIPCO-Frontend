export interface Settings {
    streamaddress?: string,
    sensitivity?: number,
    brightness?: number,
    contrast?: number,
    global_notify?: boolean,
    log_enabled?: boolean,
    fr_log_enabled?: boolean,
    cliplength?: number,
    max_logs?: number,
    max_storage?: number,
    mail_notification?: boolean,
    cam_mode?: number
}
